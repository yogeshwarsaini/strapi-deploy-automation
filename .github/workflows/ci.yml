name: Build and Push Strapi Docker Image

on:
  push:
    branches:
      - main
      - task-5  # ya aapka branch jahan se CI run karna chahte ho

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/strapi-app:latest -f task-5/Dockerfile task-5/
          docker push ${{ secrets.DOCKER_USERNAME }}/strapi-app:latest
